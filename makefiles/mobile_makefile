CC		= gcc
OBJS	= mobile/mobile.o
PROG	= mobile_user
FLAGS	= -Wall -Wextra -pthread -lpthread -D_REENTRANT

all:	${PROG}

clean:
	rm -f ${OBJS} *~

${PROG}:	${OBJS}
	${CC} ${FLAGS} ${OBJS} -o $@

.c.o:
	${CC} ${FLAGS} $< -c -o $@

